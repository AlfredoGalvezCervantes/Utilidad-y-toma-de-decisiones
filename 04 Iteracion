using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        List<int> valores = new List<int> { 1, 2, 3, 4, 5 };

        Console.WriteLine("Iteración de valores en una lista:");

        // Iteración usando un bucle for
        for (int i = 0; i < valores.Count; i++)
        {
            int valor = valores[i];
            Console.WriteLine("Valor: " + valor);
        }

        Console.WriteLine("Fin de la iteración");
    }
}
